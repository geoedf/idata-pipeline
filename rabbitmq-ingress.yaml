apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nginx-ingress
  #  annotations:
  #  nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
   - http:
      paths:
          #      - path: /rabbitmq-ui/(.*)
          # backend:
          #serviceName: rabbitmq
          #servicePort: 15672
      - backend:
          serviceName: rabbitmq
          servicePort: 15672
        path: /
     
